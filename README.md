Markdown

# Docker API

Spring Boot application with a MySQL database, configured via Docker Compose.

## Prerequisites

- Docker and Docker Compose
- Java JDK and Maven

## Getting Started

1. Start the containers using Docker Compose:
   ```bash
   docker-compose up --build